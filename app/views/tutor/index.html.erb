<h3 align="center" style="padding-top: 3em;">
  Busca asesores para materias de tu carrera
</h3>

<div class="mx-auto dynamic" align="center">
  <%= render partial: 'dropdown' %>
</div>

<div class="container" style="margin-top: 1em;">
  <ul class="list-group">
    <li class="list-group-item active">Tutores Destacados</li>
    <% @tutor.each do |tutor| %>
        <h5>
           <%= tutor.name %>
           (<font color="#36f"><%= tutor.major %></font>) - 
           <% if tutor.available? == "Unavailable" %>
               <font color="red">
           <% else %>
               <font color="lime">
           <% end %>
           <%= tutor.available?%></font>
           <%= link_to '<button type="button" class="btn btn-info">More Info</button>'.html_safe, tutor_path(tutor) %>
        </h5>
         
        <hr style="width: 100%; color: black; height: 1px; background-color:black;"/>
    <% end %>
  </ul>
</div>
